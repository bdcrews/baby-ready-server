const express = require('express');
const config = require('../config');
const {PersonalDataRecord} = require('./models');

const router = express.Router();

router.post = express.Router();

router.post('/', jsonParser,
	Produ

);